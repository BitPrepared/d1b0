version: '2'
services:

    aptcacherng:
      image: sameersbn/apt-cacher-ng:latest
      volumes:
        - ./data/apt-cacher-ng:/var/cache/apt-cacher-ng
      ports:
        - "3142:3142"
      restart: always
